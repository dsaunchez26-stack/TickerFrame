import { OptionsSidePage } from '@/components/options/OptionsSidePage';
const Puts = () => (
  <OptionsSidePage
    side="P"
    title="Put Options — Featured"
    subtitle="Downside breaks tend to be faster and more reliable. High-conviction bearish setups with pattern confirmation."
    prominent
  />
);
export default Puts;
